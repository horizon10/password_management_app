<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/light_primary</item>
        <item name="colorPrimaryDark">@color/light_primary_variant</item>
        <item name="colorAccent">@color/light_secondary</item>
        <item name="android:colorBackground">@color/light_background</item>
    </style>

    <style name="AppTheme.Dark" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/dark_primary</item>
        <item name="colorPrimaryDark">@color/dark_primary_variant</item>
        <item name="colorAccent">@color/dark_secondary</item>
        <item name="android:colorBackground">@color/dark_background</item>
    </style>
</resources>